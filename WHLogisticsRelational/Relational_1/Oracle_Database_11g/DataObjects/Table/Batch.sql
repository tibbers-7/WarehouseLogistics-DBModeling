CREATE TABLE batch (
    idbatch               INTEGER,
    quantity              INTEGER,
    expdate               DATE,
    productionreport_idpr INTEGER,
    batch_id              NUMBER NOT NULL
);